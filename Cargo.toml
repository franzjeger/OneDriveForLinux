[workspace]
members = [
    "crates/graph-client",
    "crates/sync-engine",
    "crates/vfs",
    "crates/daemon",
    "crates/tray",
    "crates/cli",
    "crates/flyout",
]
resolver = "2"

# Shared dependencies — crates reference these with `workspace = true` so
# versions and features can never drift between crates.
[workspace.dependencies]
anyhow = "1"
base64 = "0.22"
chrono = { version = "0.4", features = ["serde"] }
dirs = "5"
futures = "0.3"
libc = "0.2"
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
thiserror = "2"
toml = "0.8"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = 3
